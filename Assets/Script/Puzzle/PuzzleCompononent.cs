using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PuzzleCompononent : MonoBehaviour
{
    // Start is called before the first frame update

    public bool isFailed = false; // 실패 유무
    public bool isSolved = false; // 해결 유무

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
